# Runtime dependencies. Pinned to versions known to work on AutoDL.
# For dev tooling (pytest), use:  pip install -e ".[dev]"

torch==2.3.0
torchvision==0.18.0
timm==0.9.16

numpy==1.26.4
pandas==2.2.2
Pillow==10.3.0
matplotlib==3.8.4
scikit-image==0.23.2
tqdm==4.66.4
